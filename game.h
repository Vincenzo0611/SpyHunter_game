#include<SDL.h>

//void start_new_game(car_t* player, int* backgroundheight, int* y_map, int* distance, double* distance_d, double* speed, long long int* score);
void game(SDL_Surface* screen, SDL_Texture* scrtex, SDL_Window* window, SDL_Renderer* renderer, bool loaded);

